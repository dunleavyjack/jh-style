/** The minimum number of styles. Should always be 1. */
export const MIN_NUM_OF_STYLES = 1;

/** The maximum number of styles. */
export const MAX_NUM_OF_STYLES = 3;

/** How long each image is shown for during randomization. */
export const INTERVAL_DURATION = 150;

/** The maximum value provided to be randomized for the timeout. */
export const RANDOM_TIMEOUT_DURATION = 1500;

/** The 'base' duration of the timeout. AKA a minimum added to the randomized timeout value above. */
export const TIMEOUT_BASE_DURATION = 1000;

/** Text to be displayed on the 'randomize' button. */
export const DESTINY = 'Destiny';
