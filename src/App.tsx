import React from 'react';
import { StyleSelector } from './components/StyleSelector';

export const App = () => <StyleSelector />; // TODO: Probably can move `StyleSelector` into `App.tsx`.

export default App;
